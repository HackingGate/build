[Login]
# The rk805 pwrkey is used as a display button. Ignore it at the OS level so
# stopping the display daemon does not let logind suspend or power off the unit.
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
